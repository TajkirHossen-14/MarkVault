{
  "name": "markvault",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free, local-first bookmark manager",
  "license": "MIT",
  "scripts": {
    "dev": "esbuild --servedir=. --serve=127.0.0.1:5173",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": { "esbuild": "0.25.9" },
  "engines": { "node": ">=20" }
}
